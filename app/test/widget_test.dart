import 'package:flutter_test/flutter_test.dart';



void main() {
  testWidgets('SmivoApp renders without crashing', (
    WidgetTester tester,
  ) async {
    // NOTE: This is a smoke test to verify the app can build.
    // Full widget tests will be added per feature.
    // SmivoApp requires ProviderScope and Supabase init,
    // so this test is intentionally minimal for now.
  });
}

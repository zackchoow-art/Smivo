Carpool页面：卡片的排序，按发布时间和出发时间可切换；
拼车发布时写一个行程总预估价格；填写的座位数是发起人可以提供的空座位，需要在页面中用小字备注清楚；数量不要用下拉菜单，用带上下箭头的数字选择器，默认3，最小1，最大9；增加起始地和目的地描述，比如从学校到机场；
Carpool卡片上：显示起始地和目的地描述，而不是物理地址。
卡片上部分：左侧显示文字描述的时间和地址，右侧取消右上角的carpool标签，靠右显示一个方形的地图和路线缩略图；卡片左下角的剩余座位数量写： 3 seats available ，右下角显示单价（总预估价/总人数）
Trip Details页：
上部分卡片，起始地址和目的地都使用地址描述；
第二张卡片，起始地址和目的地都使用完整的地址：街道 + 城市 + 州 + 邮编
Departure time时间下面加一行到达时间
available seat : 3 (不要 “/3 Seats”）
rganizer下方，用户头像信息栏使用标准的用户信息组件（含消息按钮，点击直接创建群聊），点击头像后抽屉弹出拼车历史及评价；
Joined Members下方，参与人使用标准小头像组件，点击后可查看拼车历史记录及评分；这里面不要包含发起人自己。
参与者提交申请时报错：Failed to load: DatabaseException: column ub.blocker_id does
not exist ，请修复
参与者提交申请时应该被告知，当前显示的价格可能会因为总价波动、人数等原因而变化；
Add to calendar 目前点击无效，系统calendar并没有添加记录。需添加为提前1小时提醒的行程记录。
群消息会话也在聊天列表中，列表的条目显示当前群聊内所有人的头像（图片），和起始地-目的地，请创建相关的组件。
请考察是否已创建群聊的页面，可参考目前的Chatroomscreen，只需取消左上角的用户信息栏，改成所有群聊参与人的小头像组件即可；每条消息发出后，在消息旁边添加一个小的用户头像图片，在每一条消息下方显示时间的昨天添加发消息人的名字。新进群的可以看到以前的聊天记录，取消订单的不再能看到聊天列表中的这个会话；
订单成行之后，参考销售商品的order details页，生成时间线，显示每个人加入群聊的时间、退出时间、出行时间。到达目的地后，发起人填写总金额，其他参与者可以自动计算出费用。
如果用户取消订单退出，需要记录用户取消订单时距离出行时间还有多久，作为风险参考
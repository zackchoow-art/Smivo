- [ ]  sale订单详情页，已经提交过的订单，原来下面的request按钮会隐藏，并且有：已预订，预定时间，和cancel按钮，现在没有了，请恢复（参考rent订单详情页进行恢复，保持UI统一）；
- [ ]  rent商品详情页，如果买卖双方已经confirm delivery后进入租期内了会下架，但是如果客户端停留在详情页没有刷新还可以重复下单，请增加在下单按钮的检测机制，如果商品已下架则不执行；
- [ ]  请添加功能：sale和rent商品详情页的照片可以点开、可以左右滑动、可以保存到相册；
- [ ]  请在Manage transactions页面的offers卡片上添加显示：购买时间（租期），交付地址和学校名；
- [ ]  chatroomscreen页：如果图片被ai标记违规，则聊天界面会崩溃，不再刷新聊天信息，请修复；请添加功能：使聊天记录中的图片（不包括违规图片）可以保存到相册，可以在点开后左右滑动切换图片；
- [ ]  需要在所有显示图片的地方都加模糊处理机制，如果图片被ai标记，所有的图片都模糊并无法点击查看大图。包括缩略图、用户头像。可以考虑单独做一个显示图片小组件，检查平台对被标记图片的处理方式设置，如果平台设置为Blur则显示违规概述，禁止点击；如果平台设置auto reject则不加载图片，仅显示一行英文描述“该图片违规已被删除”。
- [ ]  目前后台对违规图片的blur和reject开关没有生效；无论选哪个app端都是blur处理，请在制作ap端图片显示小组件时修复这个疏忽。
- [ ]  Flagged items卡片只有缩略图模糊了，点开商品详情页没有模糊
- [ ]  管理后台：User reports 页加载用户举报的商品时信息太少，只有图片和商品名称，没有商品详情页的其他内容。请参考商品详情页添加尽可能多的内容，包括浏览人数、收藏人数、下单人数。
- [ ]  被举报的用户如果被警告，会收到多张被警告卡片（根据举报人数），请修复，并提示用户因为什么商品（或跟谁的聊天记录）被多少人举报了。
- [ ]  所有的前端用到的需要在数据库存储的开关，都在后台管理的platform settings的feature flags里添加相应开关，每个开关都添加一行描述：数据库里哪张表的哪个字段。
- [ ]  后台惩罚机制有误，管理员review时对某个举报的处理只是警告，用户却被ban了。listing和chat都是，请检查，是否在某些情况下Ban是默认值，导致管理员提交时被意外执行了Ban操作。
- [ ]  Ban records页面取消惩罚的按钮无效；
- [ ]  调查用户日活、周活、月活是否实现，如未实现请实现；
- [ ]  优化管理后台的页面布局和入口：
把Listing Review栏目改成Review栏，包括System Queue 、 User reports 、Chat reports、 User feedback、AI reviewed
- [ ]  把Push Notifications 设置为平台功能，仅平台管理员可见；
- [ ]  把Sensitive words 移动到 content moderation 去，作为平台管理的一部分，不再分学校做差异化管理，不允许学校管理员维护。
- [ ]  把settings栏目的入口改为点击右上角用户头像进入，删除左边栏的settings入口
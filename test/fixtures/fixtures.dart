/// Fake data factories for testing.
///
/// Provides realistic test data for all models. Use these instead of
/// ad-hoc data in test files to keep test data consistent.
///
/// Actual fixtures will be added as models are generated.
/// Example:
/// ```dart
/// class ListingFixtures {
///   static Listing sample() => Listing(...);
/// }
/// ```
library;
